# Secrets
.env
.env.bak*
.secrets/
answers.yaml

# Job artifacts (per-application output)
jobs/

# Runtime data + scan outputs (SQLite dedup cache, dated digests, logs)
data/
scans/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# OS
.DS_Store
answers.yaml.bak-*
# Answers he typed into forms, saved via Scout Fill (personal)
answers_saved.yaml
.answers_saved.*
